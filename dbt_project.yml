name: "claim_lineage_demo"
version: '1.0'
config-version: 2

profile: Prizm_snowflake

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  claim_lineage_demo:
    staging:
      +materialized: view
      +schema: STAGING 
    mart:
      +materialized: table
      +schema: STAGING 
seeds:
  claim_lineage_demo:
    +schema: STAGING      
  